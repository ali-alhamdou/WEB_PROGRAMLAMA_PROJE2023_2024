@using EntityLayer.Concrete
@model List<Doctor>
@{
    ViewData["Title"] = "Doctors";
    Layout = "~/Views/Shared/_AdminPanel.cshtml";
}


<div class="container">
    <h4><strong>@_localization.Getkey("str_Doctors")</strong></h4><hr />
    <div class="row justify-content-center">
        @foreach (var item in Model)
        {

            <div class="col-2 m-1 py-2 px-3 border border-dark text-center bg-white">
                <img src="@item.Image" width="50px" />
                <hr />
                <strong>@item.DoctorName</strong>
                <br />
                <i>@item.Department.DepartmentName</i>
                <hr />
                <a class="btn btn-outline-primary" href="/Admin/CheckDoctor/@item.DoctorID">@_localization.Getkey("str_SeeMore")</a>
            </div>

        }
    </div>
</div>

